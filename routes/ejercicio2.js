var express = require('express');
var router = express.Router();

/* GET users listing. */
router.post('/asteriscos', function(req, res, next) {
  var params = req.body;

});

module.exports = router;
